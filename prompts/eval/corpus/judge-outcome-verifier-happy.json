{
  "id": "judge.outcome-verifier.happy.v1",
  "role": "judge",
  "tags": ["judge", "outcome", "happy-path"],
  "difficulty": "easy",
  "input": {
    "extraVariables": {
      "expected_outcomes": "- cart badge shows 3\n- cart total equals sum of item prices",
      "test_source": "import { test, expect } from '@playwright/test';\ntest('cart', async ({ page }) => {\n  await page.goto('/');\n  await expect(page.getByTestId('cart-badge')).toHaveText('3');\n  await expect(page.getByTestId('cart-total')).toHaveText(/^\\$?\\d+/);\n});",
      "page_object_source": ""
    }
  },
  "expected": {
    "judgeVerdict": {
      "allCovered": true,
      "minConfidence": 0.8
    }
  },
  "metrics": {
    "outcomeCoverageTarget": 1.0,
    "costMaxUSD": 0.05,
    "latencyMaxMs": 10000
  }
}
