{
  "name": "@poc/rls-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tables.test.js",
    "db:reset": "bash scripts/reset-db.sh",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@poc/types": "*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
